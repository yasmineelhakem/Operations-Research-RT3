[theme]

#primaryColor = '#2792E3'
backgroundColor = '#273346'
secondaryBackgroundColor = '#616569'
textColor = '#FFFFFF'

# Font family for all text in the app, except code blocks
# Accepted values (serif | sans serif | monospace)
font = "serif"